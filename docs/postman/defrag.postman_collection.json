{
  "info": { "name": "DEFRAG API", "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json" },
  "item": [
    { "name": "Health", "request": { "method": "GET", "url": "https://api.defrag.app/healthz" } },
    {
      "name": "Root (auth)",
      "request": {
        "method": "GET",
        "url": "https://api.defrag.app/",
        "header": [{ "key": "x-api-key", "value": "{{X_API_KEY}}" }]
      }
    },
    {
      "name": "Classify (stub)",
      "request": {
        "method": "POST",
        "url": "https://api.defrag.app/classify",
        "header": [
          { "key": "x-api-key", "value": "{{X_API_KEY}}" },
          { "key": "content-type", "value": "application/json" }
        ],
        "body": { "mode": "raw", "raw": "{\"text\":\"hello\"}" }
      }
    }
  ],
  "variable": [{ "key": "X_API_KEY", "value": "" }]
}